<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.CategoryDao">
    <resultMap id="BaseResultMap" type="entity.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
    </resultMap>

    <insert id="insert" parameterType="entity.Category" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name) VALUES (#{name})
    </insert>

    <update id="update" parameterType="entity.Category">
        UPDATE category SET name = #{name} WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="int">
        DELETE FROM category WHERE id = #{id}
    </delete>

    <select id="selectById" parameterType="int" resultMap="BaseResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM category
    </select>

    <select id="selectByName" parameterType="string" resultMap="BaseResultMap">
        SELECT * FROM category WHERE name LIKE CONCAT('%', #{name}, '%')
    </select>
</mapper>